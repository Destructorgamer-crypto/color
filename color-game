<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Color Matcher</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            user-select: none;
        }

        body {
            background-color: #0d0f17;
            color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }

        #game-container {
            width: 450px;
            background: #161925;
            padding: 30px;
            border-radius: 16px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.5), 0 0 0 1px rgba(255,255,255,0.05);
            text-align: center;
        }

        h1 {
            font-size: 24px;
            margin-bottom: 20px;
            letter-spacing: 0.5px;
            color: #f3f4f6;
        }

        /* Pantallas */
        .screen {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        #color-display {
            width: 100%;
            height: 200px;
            border-radius: 10px;
            margin-bottom: 20px;
            transition: background-color 0.3s ease;
            box-shadow: inset 0 0 0 1px rgba(255,255,255,0.1);
        }

        /* Paneles de control */
        .control-group {
            width: 100%;
            margin-bottom: 20px;
            text-align: left;
        }

        .control-label {
            display: flex;
            justify-content: space-between;
            font-size: 14px;
            color: #9ca3af;
            margin-bottom: 6px;
        }

        input[type=range] {
            -webkit-appearance: none;
            width: 100%;
            height: 8px;
            border-radius: 4px;
            background: #252a3d;
            outline: none;
        }

        input[type=range]::-webkit-slider-thumb {
            -webkit-appearance: none;
            appearance: none;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background: #ffffff;
            cursor: pointer;
            transition: transform 0.1s;
        }

        input[type=range]::-webkit-slider-thumb:hover {
            transform: scale(1.15);
        }

        /* Sliders personalizados por color */
        #hue::-webkit-slider-thumb { background: linear-gradient(90deg, red, yellow, green, cyan, blue, magenta, red); }
        #saturation::-webkit-slider-thumb { background: #6366f1; }
        #lightness::-webkit-slider-thumb { background: #eab308; }

        /* Vista previa del usuario */
        #user-color-preview {
            width: 100%;
            height: 80px;
            border-radius: 8px;
            margin-bottom: 20px;
            box-shadow: inset 0 0 0 1px rgba(255,255,255,0.1);
        }

        /* Botón estilo dialed.gg */
        button {
            background: #1f2937;
            color: #f9fafb;
            border: 1px solid #374151;
            padding: 12px 24px;
            font-size: 16px;
            font-weight: 500;
            border-radius: 8px;
            cursor: pointer;
            width: 100%;
            transition: all 0.2s ease;
        }

        button:hover {
            background: #374151;
            border-color: #6366f1;
            box-shadow: 0 0 15px rgba(99, 102, 241, 0.3);
        }

        /* Resultados y Contadores */
        .round-counter {
            font-size: 14px;
            color: #9ca3af;
            margin-bottom: 15px;
        }

        #timer {
            font-size: 32px;
            font-weight: bold;
            color: #ef4444;
            margin-bottom: 20px;
            font-variant-numeric: tabular-nums;
        }

        #feedback {
            font-size: 20px;
            font-weight: 600;
            margin-bottom: 15px;
            color: #10b981;
        }

        #round-history {
            display: flex;
            justify-content: center;
            gap: 10px;
            font-size: 14px;
            color: #6b7280;
            margin-top: 15px;
            font-variant-numeric: tabular-nums;
        }

        #final-score-display {
            font-size: 48px;
            font-weight: bold;
            background: linear-gradient(90deg, #6366f1, #06b6d4);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>

    <div id="game-container">
        <div id="start-screen" class="screen">
            <h1 style="font-size: 28px;">COLOR MATCHER</h1>
            <p style="color: #9ca3af; margin-bottom: 25px; font-size: 14px; line-height: 1.4;">
                Memoriza el color aleatorio durante 5 segundos. Luego, ajusta el tono, saturación y luminosidad para replicarlo.
            </p>
            <button onclick="starGameFlow()">JUGAR</button>
        </div>

        <div id="memorize-screen" class="screen" style="display: none;">
            <div class="round-counter">Ronda <span id="m-round">1</span> / 5</div>
            <div id="timer">5</div>
            <div id="color-display"></div>
            <p style="color: #9ca3af; font-size: 13px;">Memorizando color...</p>
        </div>

        <div id="guess-screen" class="screen" style="display: none;">
            <div class="round-counter">Ronda <span id="g-round">1</span> / 5</div>
            <div id="user-color-preview"></div>
            
            <div class="control-group">
                <div class="control-label"><span>Tono (Hue)</span><span id="h-val">0°</span></div>
                <input type="range" id="hue" min="0" max="360" value="0" oninput="updateColor()">
            </div>

            <div class="control-group">
                <div class="control-label"><span>Saturación</span><span id="s-val">0%</span></div>
                <input type="range" id="saturation" min="0" max="100" value="50" oninput="updateColor()">
            </div>

            <div class="control-group">
                <div class="control-label"><span>Luminosidad</span><span id="l-val">0%</span></div>
                <input type="range" id="lightness" min="0" max="100" value="50" oninput="updateColor()">
            </div>

            <button onclick="submitGuess()" style="margin-top: 10px;">CONFIRMAR</button>
        </div>

        <div id="round-result-screen" class="screen" style="display: none;">
            <div class="round-counter">Resultado Ronda <span id="r-round">1</span></div>
            <div id="feedback">¡Precisión: 8.5/10!</div>
            
            <div style="display: flex; gap: 20px; margin-bottom: 20px;">
                <div>
                    <div style="font-size: 12px; color: #9ca3af; margin-bottom: 5px;">Original</div>
                    <div id="res-original" style="width: 80px; height: 80px; border-radius: 8px;"></div>
                </div>
                <div>
                    <div style="font-size: 12px; color: #9ca3af; margin-bottom: 5px;">Tu respuesta</div>
                    <div id="res-guess" style="width: 80px; height: 80px; border-radius: 8px;"></div>
                </div>
            </div>

            <button id="next-btn" onclick="nextStep()">Siguiente Ronda</button>
        </div>

        <div id="gameover-screen" class="screen" style="display: none;">
            <h1 style="margin-bottom: 5px;">PARTIDA FINALIZADA</h1>
            <div id="final-score-display">42.50 / 50</div>
            <p style="color: #9ca3af; font-size: 14px; margin-bottom: 20px;">Puntuación total acumulada</p>
            
            <div style="background: #1f2937; padding: 12px 16px; border-radius: 8px; width: 100%; font-size: 13px; color: #d1d5db;">
                Desglose por rondas:
                <div id="round-history"></div>
            </div>

            <button onclick="starGameFlow()" style="margin-top: 20px;">VOLVER A JUGAR</button>
        </div>
    </div>

    <script>
        let targetH, targetS, targetL;
        let currentH = 0, currentS = 50, currentL = 50;
        let round = 1;
        let scores = [];
        let timerInterval;

        // Elementos DOM
        const startScreen = document.getElementById('start-screen');
        const memorizeScreen = document.getElementById('memorize-screen');
        const guessScreen = document.getElementById('guess-screen');
        const roundResultScreen = document.getElementById('round-result-screen');
        const gameOverScreen = document.getElementById('gameover-screen');

        function starGameFlow() {
            round = 1;
            scores = [];
            startScreen.style.display = 'none';
            gameOverScreen.style.display = 'none';
            runRound();
        }

        function generateRandomColor() {
            targetH = Math.floor(Math.random() * 361);
            targetS = Math.floor(Math.random() * 41) + 30;  // Mantener saturación media/alta para que sea visible
            targetL = Math.floor(Math.random() * 41) + 30;  // Mantener luminosidad media
        }

        function runRound() {
            // Ocultar otras pantallas por si acaso
            memorizeScreen.style.display = 'flex';
            guessScreen.style.display = 'none';
            roundResultScreen.style.display = 'none';
            
            document.getElementById('m-round').innerText = round;
            generateRandomColor();
            
            const displayBox = document.getElementById('color-display');
            displayBox.style.backgroundColor = `hsl(${targetH}, ${targetS}%, ${targetL}%, 1)`;

            let timeLeft = 5;
            document.getElementById('timer').innerText = timeLeft;

            clearInterval(timerInterval);
            timerInterval = setInterval(() => {
                timeLeft--;
                document.getElementById('timer').innerText = timeLeft;

                if (timeLeft <= 0) {
                    clearInterval(timerInterval);
                    openGuessScreen();
                }
            }, 1000);
        }

        function openGuessScreen() {
            memorizeScreen.style.display = 'none';
            guessScreen.style.display = 'flex';
            
            document.getElementById('g-round').innerText = round;
            
            // Resetear inputs a valores neutros
            currentH = 0; currentS = 50; currentL = 50;
            document.getElementById('hue').value = currentH;
            document.getElementById('saturation').value = currentS;
            document.getElementById('lightness').value = currentL;
            
            updateColor();
        }

        function updateColor() {
            currentH = document.getElementById('hue').value;
            currentS = document.getElementById('saturation').value;
            currentL = document.getElementById('lightness').value;

            document.getElementById('h-val').innerText = `${currentH}°`;
            document.getElementById('s-val').innerText = `${currentS}%`;
            document.getElementById('l-val').innerText = `${currentL}%`;

            document.getElementById('user-color-preview').style.backgroundColor = `hsl(${currentH}, ${currentS}%, ${currentL}%)`;
        }

        function calculateScore() {
            // Diferencia máxima posible: H=180, S=100, L=100
            const diffH = Math.min(Math.abs(targetH - currentH), 360 - Math.abs(targetH - currentH)); // Distancia circular en el tono
            const diffS = Math.abs(targetS - currentS);
            const diffL = Math.abs(targetL - currentL);

            // Penalización normalizada sobre 10 puntos (cuanto menor sea la diferencia, más suma)
            const maxDiff = 180 + 100 + 100;
            const totalDiff = diffH + diffS + diffL;
            
            // Fórmula que da 10 si es perfecto y baja progresivamente
            let roundScore = 10 - ((totalDiff / maxDiff) * 10);
            return Math.max(0, parseFloat(roundScore.toFixed(2)));
        }

        function submitGuess() {
            const score = calculateScore();
            scores.push(score);

            guessScreen.style.display = 'none';
            roundResultScreen.style.display = 'flex';

            document.getElementById('r-round').innerText = round;
            document.getElementById('feedback').innerText = `Precisión: ${score} / 10`;

            document.getElementById('res-original').style.backgroundColor = `hsl(${targetH}, ${targetS}%, ${targetL}%)`;
            document.getElementById('res-guess').style.backgroundColor = `hsl(${currentH}, ${currentS}%, ${currentL}%)`;

            // Cambiar texto de botón si es la ronda 5
            if (round === 5) {
                document.getElementById('next-btn').innerText = "Ver Resultados Finales";
            } else {
                document.getElementById('next-btn').innerText = "Siguiente Ronda";
            }
        }

        function nextStep() {
            if (round < 5) {
                round++;
                runRound();
            } else {
                showGameOver();
            }
        }

        function showGameOver() {
            roundResultScreen.style.display = 'none';
            gameOverScreen.style.display = 'flex';

            const total = scores.reduce((acc, curr) => acc + curr, 0).toFixed(2);
            document.getElementById('final-score-display').innerText = `${total} / 50`;

            // Generar el formato pequeño de cada ronda separado por espacios
            document.getElementById('round-history').innerText = scores.join('  ');
        }
    </script>
</body>
</html>
